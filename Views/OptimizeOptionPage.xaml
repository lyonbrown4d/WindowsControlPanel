<UserControl x:Class="WindowsControlPanel.Views.OptimizeOptionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="650"
             d:DesignWidth="980">
    <Grid Background="Transparent">
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

            <Style x:Key="SidebarButtonStyle" TargetType="ui:Button">
                <Setter Property="Height" Value="42" />
                <Setter Property="Appearance" Value="Secondary" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="12,0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{StaticResource AppStrokeBrush}" />
                <Setter Property="Background" Value="{StaticResource AppSurfaceElevatedBrush}" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="276" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource AppSurfaceBrush}"
                BorderBrush="{StaticResource AppStrokeBrush}"
                BorderThickness="0,0,1,0">
            <Grid Margin="16,18,16,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Style="{StaticResource AppEyebrowTextStyle}" Text="OPTIMIZE" />
                    <TextBlock Margin="0,5,0,0" Style="{StaticResource AppPageTitleTextStyle}" Text="进一步优化" />
                    <TextBlock Margin="0,7,0,0"
                               Style="{StaticResource AppBodyTextStyle}"
                               Text="集中处理安全、开发、游戏、启动项、磁盘和网络相关的高级控制。" />
                </StackPanel>

                <Border Grid.Row="1"
                        Margin="0,16,0,18"
                        Padding="12"
                        CornerRadius="8"
                        Background="{StaticResource AppSurfaceElevatedBrush}"
                        BorderBrush="{StaticResource AppStrokeBrush}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="8"
                                Height="8"
                                Margin="0,4,10,0"
                                CornerRadius="4"
                                Background="{StaticResource AppSuccessBrush}" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Style="{StaticResource AppSmallLabelTextStyle}" Text="当前工作区" />
                            <TextBlock Margin="0,5,0,0"
                                       FontSize="12"
                                       LineHeight="18"
                                       Text="{Binding GlobalStatus}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2">
                    <TextBlock Margin="0,0,0,9" Style="{StaticResource AppSmallLabelTextStyle}" Text="模块" />

                    <Grid Margin="0,0,0,9">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsSecuritySelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenSecurityCommand}" Content="安全与虚拟化">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="ShieldLock24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSecuritySelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>

                    <Grid Margin="0,0,0,9">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsDevelopmentSelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenDevelopmentCommand}" Content="开发环境">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Code24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDevelopmentSelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>

                    <Grid Margin="0,0,0,9">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsGameSelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenGameCommand}" Content="游戏优化">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Games24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGameSelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>

                    <Grid Margin="0,0,0,9">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsStartupSelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenStartupCommand}" Content="启动与服务">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Rocket24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStartupSelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>

                    <Grid Margin="0,0,0,9">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsCleanupSelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenCleanupCommand}" Content="磁盘与清理">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Delete24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCleanupSelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2" Background="{StaticResource AppAccentBrush}" Visibility="{Binding IsNetworkSelected, Converter={StaticResource BoolToVisibility}}" />
                        <ui:Button Grid.Column="1" Command="{Binding OpenNetworkCommand}" Content="网络与 DNS">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Globe24" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsNetworkSelected}" Value="True">
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                                            <Setter Property="Background" Value="{StaticResource AppAccentSoftBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="3">
                    <Border Margin="0,0,0,10"
                            Padding="10"
                            CornerRadius="8"
                            Background="{StaticResource AppSurfaceSubtleBrush}"
                            BorderBrush="{StaticResource AppStrokeBrush}"
                            BorderThickness="1">
                        <TextBlock Style="{StaticResource AppBodyTextStyle}" Text="高风险操作会先确认；需要管理员权限的动作会明确提示。" />
                    </Border>
                    <ui:Button Height="36"
                               Appearance="Secondary"
                               Command="{Binding BackToHomeCommand}"
                               Content="返回首页">
                        <ui:Button.Icon>
                            <ui:SymbolIcon Symbol="Home24" />
                        </ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Margin="14"
                Background="{StaticResource AppSurfaceSubtleBrush}"
                BorderBrush="{StaticResource AppStrokeBrush}"
                BorderThickness="1"
                CornerRadius="10"
                ClipToBounds="True">
            <ContentControl regions:RegionManager.RegionName="FeatureRegion"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" />
        </Border>
    </Grid>
</UserControl>
